package repository;

import swimapp.domain.Swimmer;

public interface SwimRepository extends ICrudRepository<Integer, Swimmer> {
//    Iterable<SortingTask> filterBy(SortingOrder order);
//    Iterable<SortingTask> filterBy(SortingAlgorithm algo);
}
